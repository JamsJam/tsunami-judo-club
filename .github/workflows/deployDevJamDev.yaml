name: deployDev
on:
  push:
    branches: [develop]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.APP__HOST }}
          username: ${{ secrets.APP__USER }}
          password: ${{ secrets.APP__PASSWORD }}
          port: ${{ secrets.PORT }}
          script: |
            cd /home/jam/web/tsunamidev.jamdev.fr/public_html/tsunami-judo-club; echo "Pulling the latest changes from the git repository..."; git pull; echo "Installing composer dependencies..."; COMPOSER_ALLOW_SUPERUSER=1 composer install --no-dev --optimize-autoloader -n; echo "Clearing the cache for production environment..."; APP_ENV=prod APP_DEBUG=0 php bin/console cache:clear; echo "Running database migrations..."; php bin/console d:m:m -n; echo "Installing JavaScript dependencies using Importmap..."; php bin/console importmap:install; echo "Building SASS files..."; php bin/console sass:build; echo "Compiling asset map..."; php bin/console asset-map:compile

      


# COMPOSER_ALLOW_SUPERUSER=1 composer require symfony/requirement-checker ; nano .env.prod.local; COMPOSER_ALLOW_SUPERUSER=1 composer dump-env prod ; COMPOSER_ALLOW_USERUSER=1 composer install --no-dev --optimize-autoloader ; APP_ENV=prod APP_DEBUG=0 php bin/console cache:clear ; php bin/console d:d:d --force ; php d:d:c ; php d:m:m -n
# COMPOSER_ALLOW_SUPERUSER=1 composer require symfony/requirements-checker ; COMPOSER_ALLOW_SUPERUSER=1 composer dump-env prod ; COMPOSER_ALLOW_SUPERUSER=1 composer install --no-dev --optimize-autoloader ; APP_ENV=prod APP_DEBUG=0 php bin/console cache:clear ; php bin/console d:d:d --force ; php bin/console d:d:c ; php bin/console d:m:m -n; php bin/console sass:build ; php bin/console asset-map:compile